<section>
    <h1>Find your next adventure!</h1>
    <h2>What are you looking to do?</h2>

    <form class="search-attraction-form">
        <div class="attraction-category">
            <label for="attraction-category">Category:</label>
            <select name="attraction-category" id="attraction-category">
                <option value=" "> </option>
                <option value=1>Aliens</option>
                <option value=2>Cryptids</option>
                <option value=3>Hauntings</option>
                <option value=4>Witches</option>
            </select>
        </div>
        <div class="attraction-type">
            <label for="attraction-type">Type:</label>
            <select name="attraction-type" id="attraction-type">
                <option value=" "> </option>
                <option value=1>Museum</option>
                <option value=2>Cemetary</option>
                <option value=3>Historical Site</option>
                <option value=4>Sighting Location</option>
                <option value=5>Asylum</option>
                <option value=6>Prison</option>
                <option value=7>Walking Tour</option>
            </select>
        </div>
        <div>
            <label for="attraction-location">Near</label>
            <input type="text" id="attraction-location" name="attraction-location" placeholder="Enter a location..."/>
        </div>
        <button type="submit" class="btn">Search</button>
    </form>

    {{!-- not working yet --}}
    <div id="map"></div>
    
    <h2>Not sure what to search for? Check out one of these attractions:</h2>

    {{!-- Pull attractions --}}
    <ul class="attraction-list">
        {{#each attractions as |attraction|}}
        <li>
            <article class="attraction">
            <div class="title">
                <a href="/attractions/{{id}}">{{name}}</a>
            </div>
            </article>
        </li>
        {{/each}}
    </ul>

</section>

{{!-- Basic map funcationality, will add more later --}}
<script src="/js/map-init.js"></script>

{{!-- Search not working yet --}}
<script src="/js/search-attractions.js"></script>